"use strict";

var _constants = require("./constants");

var FROZEN_INTERNAL_TYPE = Object.freeze(_constants.INTERNAL_TYPE);
var FROZEN_PREFIX = Object.freeze(_constants.PREFIX); // [1] These purposefully reference $Values instead of $Keys