import { jsx as ___EmotionJSX } from "@emotion/core";

function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; var ownKeys = Object.keys(source); if (typeof Object.getOwnPropertySymbols === 'function') { ownKeys = ownKeys.concat(Object.getOwnPropertySymbols(source).filter(function (sym) { return Object.getOwnPropertyDescriptor(source, sym).enumerable; })); } ownKeys.forEach(function (key) { _defineProperty(target, key, source[key]); }); } return target; }

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

function _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; subClass.__proto__ = superClass; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

import React, { Component } from 'react';
import { isRenderProp } from "../utils";
import IconArrowDropdownDown from "../Icon/IconArrowDropdownDown";
import IconArrowDropdownUp from "../Icon/IconArrowDropdownUp";
import TableContext from "./TableContext";
import { TableSortableHeaderCellRoot as Root, TableSortableHeaderCellButton as Button, TableSortableHeaderCellContent as Content, TableSortableHeaderCellIconHolder as IconHolder } from "./styled";
import { SORT } from "./constants";
var iconProps = {
  'aria-hidden': true,
  size: 'auto'
};
var sortIcon = {
  ascending: ___EmotionJSX(IconArrowDropdownUp, iconProps),
  descending: ___EmotionJSX(IconArrowDropdownDown, iconProps)
};

var TableSortableHeaderCell =
/*#__PURE__*/
function (_Component) {
  _inheritsLoose(TableSortableHeaderCell, _Component);

  function TableSortableHeaderCell() {
    return _Component.apply(this, arguments) || this;
  }

  var _proto = TableSortableHeaderCell.prototype;

  _proto.render = function render() {
    var _this = this;

    return ___EmotionJSX(TableContext.Consumer, null, function (tableContextProps) {
      var _this$props = _this.props,
          children = _this$props.children,
          label = _this$props.label,
          name = _this$props.name,
          messages = _this$props.messages,
          render = _this$props.render,
          sortable = _this$props.sortable,
          restProps = _objectWithoutPropertiesLoose(_this$props, ["children", "label", "name", "messages", "render", "sortable"]);

      var sort = sortable.sort,
          sortFn = sortable.sortFn;
      var sortColumn = sort && sort.key;
      var descending = sort ? sort.descending : false;
      var direction = descending ? SORT.descending : SORT.ascending;
      var isSorted = sortColumn === name;
      var nextDirection = isSorted && direction === SORT.ascending ? SORT.descending : SORT.ascending;
      var toggle = isSorted || direction !== nextDirection;
      var nextDescending = toggle ? !descending : descending;
      var nextSort = {
        key: name,
        descending: nextDescending
      };

      var handleSort = function handleSort() {
        return sortFn(nextSort);
      };

      var rootProps = _objectSpread({
        'aria-label': label || children,
        'aria-sort': isSorted ? direction : 'none',
        role: 'columnheader'
      }, restProps);

      var buttonProps = _objectSpread({
        'aria-label': nextDirection === SORT.ascending ? messages.sortColumnAscending : messages.sortColumnDescending,
        onClick: handleSort
      }, restProps);

      var iconHolderProps = {
        direction: isSorted && direction || SORT.ascending,
        isSorted: isSorted
      };

      if (isRenderProp(render)) {
        var renderProps = {
          props: _objectSpread({}, tableContextProps, rootProps, {
            children: children,
            label: label,
            name: name,
            messages: messages,
            sortable: sortable
          }),
          state: {
            direction: direction,
            isSorted: isSorted
          },
          helpers: {
            handleSort: handleSort
          }
        };
        return render(renderProps);
      }

      return ___EmotionJSX(Root, rootProps, ___EmotionJSX(Button, buttonProps, ___EmotionJSX(Content, null, children), "\xA0", ___EmotionJSX(IconHolder, iconHolderProps, sortIcon[iconHolderProps.direction])));
    });
  };

  return TableSortableHeaderCell;
}(Component);

_defineProperty(TableSortableHeaderCell, "displayName", 'TableSortableHeaderCell');

export { TableSortableHeaderCell as default };