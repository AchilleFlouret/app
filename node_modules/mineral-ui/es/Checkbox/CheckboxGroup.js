import { jsx as ___EmotionJSX } from "@emotion/core";

function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; var ownKeys = Object.keys(source); if (typeof Object.getOwnPropertySymbols === 'function') { ownKeys = ownKeys.concat(Object.getOwnPropertySymbols(source).filter(function (sym) { return Object.getOwnPropertyDescriptor(source, sym).enumerable; })); } ownKeys.forEach(function (key) { _defineProperty(target, key, source[key]); }); } return target; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

import React from 'react';
import Checkbox from "./Checkbox";
import { CheckboxGroupRoot as Root } from "./styled";
import { checkboxGroupPropTypes } from "./propTypes";

var CheckboxGroup = function CheckboxGroup(props) {
  var otherRootProps = props.rootProps,
      restProps = _objectWithoutPropertiesLoose(props, ["rootProps"]);

  var rootProps = _objectSpread({
    rootProps: _objectSpread({}, otherRootProps),
    input: Checkbox,
    type: 'checkbox'
  }, restProps);

  return ___EmotionJSX(Root, rootProps);
};

CheckboxGroup.displayName = 'CheckboxGroup';
CheckboxGroup.propTypes = process.env.NODE_ENV !== "production" ? checkboxGroupPropTypes : {};
export default CheckboxGroup;