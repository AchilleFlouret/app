import PopperJs from 'popper.js';
export var ARROW_SIZE = '8px';
export var PLACEMENT = PopperJs.placements.sort().reduce(function (acc, placement) {
  acc[placement] = placement;
  return acc;
}, {});