import { ALIGN, INTERNAL_TYPE, PREFIX, TYPE } from "./constants";
var FROZEN_INTERNAL_TYPE = Object.freeze(INTERNAL_TYPE);
var FROZEN_PREFIX = Object.freeze(PREFIX); // [1] These purposefully reference $Values instead of $Keys